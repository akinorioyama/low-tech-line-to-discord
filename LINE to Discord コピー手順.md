# **LINE to Discord データコピー手順書**

この手順書は、LINEのメッセージデータ（TSV形式）を読み込み、添付ファイルと共にDiscordサーバーへ転送するプログラムの実行手順を説明します。一部手作業でのファイル移動・更新が含まれます。

> プログラムが内部で何をしているか（重複排除・引用リンク・対応表の仕組みなど）は、別紙
> [LINE to Discord 内部処理の説明.md](LINE%20to%20Discord%20%E5%86%85%E9%83%A8%E5%87%A6%E7%90%86%E3%81%AE%E8%AA%AC%E6%98%8E.md)
> を参照してください。

## **1\. 事前準備**

実行前に以下のファイルとフォルダを用意してください。

* **プログラム本体**: read\_line\_tsv.py  
* **LINEデータ**: LINEから出力されたTSVファイル（例: line\_2024.tsv）  
* **設定ファイル (.ini)**: DiscordのWebhook URLやサーバー情報、LINEのトークン、対象グループIDを記載したファイル（例: config\_a.ini）  
* **各種フォルダ**:  
  * All\_Payloads\_a など (過去のペイロードを格納するフォルダ。対象のグループIDごとに専用のフォルダを用意してください)  
  * FILE\_CACHE (キャッシュ画像を格納するフォルダ)

### **【手作業】データの事前準備**

1. **キャッシュファイルの準備**: 別途Google Drive等に自動格納しておいた添付ファイルをダウンロードし、FILE\_CACHE フォルダ内に展開しておきます。  
2. **ペア情報（JSON）ファイルのマージ**:

   プログラムは「LINE のメッセージ ID」と「送信先 Discord のメッセージ ID」の
   対応表を JSON ファイルで管理します。引用（返信）メッセージに正しいリンクを
   貼るため、**過去の対応表を次回実行に引き継ぐ**必要があります。

   ファイルは次の 2 種類があります（`\<groupId\>` は対象グループの ID）。

   | ファイル名 | 役割 |
   | --- | --- |
   | `output_message_pair_\<groupId\>.json` | **マスター（累積）ファイル**。過去すべての対応をためていく台帳で、実行開始時にプログラムが読み込む。 |
   | `output_message_pair_\<groupId\>_\<日時\>.json` | **直近の実行結果ファイル**。その回に新しく送信した分の対応だけが入る。途中経過は末尾 `_temp` 付き、完了版は `_temp` なし。 |

   **マージ手順:**

   1. 直近の実行で出力された `output_message_pair_\<groupId\>_\<日時\>.json`
      （途中で止まった場合は `..._temp.json`）を開く。
   2. その中の**新規エントリー**を、マスター
      `output_message_pair_\<groupId\>.json` にコピーして統合（マージ）する。
   3. 次回実行時はこのマスターが読み込まれ、過去メッセージへの引用リンクが有効になる。

   各 JSON ファイルの中身は、`{ "LINEのメッセージID": "DiscordのメッセージID", ... }`
   という対応が**送信順に並んだ**形になっています。マスターはある時点までの分、
   最終結果ファイルはそれに続く新規分まで含むため、**マスターの末尾キーを目印に
   差分を切り出して追加**します。
   （対応表の役割や、なぜ自動マージされないのかは
   [内部処理の説明](LINE%20to%20Discord%20%E5%86%85%E9%83%A8%E5%87%A6%E7%90%86%E3%81%AE%E8%AA%AC%E6%98%8E.md)
   を参照。）

   #### マージの具体例（個別集団グループの実例）

   * 累積していくマスター: `output_message_pair_\<個別集団\>.json`（この例では 124 件）
   * 最終結果を保持: `output_message_pair_\<個別集団\>_\<日時\>_temp.json`（この例では 172 件）

   **手順 1: マスターの最終行（最後のキー）を確認する。**

   ```json
   // output_message_pair_<個別集団>.json （マスター・末尾）
       ...
       "100000000000000010": "2000000000000000010",
       "100000000000000020": "2000000000000000020"   ← 最終行。キー側ID = 100000000000000020
   }
   ```

   **手順 2: 最終結果ファイルの中から、同じキー `100000000000000020` を探す。**
   そのキー自身はマスターに既にあるので**含めず**、それ以降（次の行から）を差分とする。

   ```json
   // output_message_pair_<個別集団>_<日時>_temp.json （該当キー周辺）
       ...
       "100000000000000020": "2000000000000000020",   ← マスターと同じ。ここまでは重複
       "100000000000000030": "2000000000000000030",   ← ここから下が新規（追加対象）
       "100000000000000040": "2000000000000000040",
       "100000000000000050": "2000000000000000050",
       ...
       "100000000000000060": "2000000000000000060"     ← 最終結果ファイルの末尾
   }
   ```

   **手順 3: 切り出した新規分（この例では 48 件）を、マスターの最終行の後ろに追加する。**

   ```json
   // output_message_pair_<個別集団>.json （マージ後・124 + 48 = 172 件）
       ...
       "100000000000000020": "2000000000000000020",   ← 旧・最終行（末尾に「,」を追加）
       "100000000000000030": "2000000000000000030",   ← ここから追記
       ...
       "100000000000000060": "2000000000000000060"
   }
   ```

   > **ポイント**
   > * 目印にするのは「マスターの**最後のキー**」。そのキーを最終結果ファイルの中で見つけ、
   >   **その行は除いて、次の行以降**をコピーする。
   > * マスター旧末尾の行末に区切りの `,` を付け忘れない（JSON として壊れないように）。
   > * 件数で確認できる（例: マスター 124 + 新規 48 = 最終結果 172）。

## **2\. 実行手順**

本プログラムは、安全に移行を行うため **「データの抽出」** と **「Discordへの送信」** の2フェーズに分けて実行します。

### **フェーズ1: データの抽出とダウンロード**

LINEデータを読み込み、メッセージや画像ファイルなどをローカルフォルダに抽出します。

**コマンド構文:**

python read\_line\_tsv.py \<設定ファイル\> \<TSVファイル\> \<ペイロードフォルダ\> \<抽出先フォルダ\> \<キャッシュフォルダ\>

**実行例:**

python read\_line\_tsv.py "config\_a.ini" "line\_2024.tsv" "All\_Payloads\_a" "LINE\_2024" "FILE\_CACHE"

* 指定した抽出先フォルダ（LINE\_2024）にデータが保存されます。

**【手作業】ペイロードフォルダへのコピー**

フェーズ1の終了後、抽出先フォルダ（LINE\_2024）に作成されたすべてのファイルを、対象グループ用のペイロードフォルダ（例: All\_Payloads\_a）に手動でコピーしてください。

### **フェーズ2: Discordサーバーへの送信**

抽出したデータを元に、設定ファイルで指定されたDiscordチャンネルへメッセージとファイルを送信します。

**コマンド構文:**

python read\_line\_tsv.py \<設定ファイル\> \<TSVファイル\> \<ペイロードフォルダ\>

*(抽出先フォルダ・キャッシュフォルダの指定を外します)*

**実行例:**

python read\_line\_tsv.py "config\_a.ini" "line\_2024.tsv" "All\_Payloads\_a"

* 送信が完了すると、新規のメッセージID紐付け情報が output\_message\_pair\_\<対象グループID\>\_\<日時\>.json として保存されます。（次回実行時は、このファイルの内容を「事前準備」の手順でマージしてください）

## **3\. 複数サーバーへの対応（任意）**

複数のLINEチャンネル/Discordサーバーに送信する場合は、サーバー（グループ）ごとの設定ファイル（例: config\_b.ini）と専用のペイロードフォルダ（例: All\_Payloads\_b）を作成し、フェーズ1とフェーズ2の手順を繰り返してください。

### フォルダ構成の例（2 グループ運用）

以下は、性質の異なる 2 グループ ―― **全体グループ**（全員が参加する大きいグループ）と
**個別集団グループ**（一部のメンバーだけの小さいグループ）―― を運用する場合の構成例です。
（`<全体>` / `<個別集団>` の部分は、実際の groupId に読み替えてください。）

```
<プロジェクトルート>/                    # 作業フォルダ（任意の場所でよい）
├─ read_line_tsv.py                      # 本体スクリプト
├─ read_line_tsv_split_by_group.py       # TSV をグループ毎に分割するスクリプト
│
├─ config_<全体>.ini                     # 全体グループ用の設定ファイル
├─ config_<個別集団>.ini                 # 個別集団グループ用の設定ファイル
│
├─ Line record/                          # LINE 元データ(TSV)置き場（分割前・分割後）
│   ├─ LINE record - upto_<日時>.tsv                        # 全グループ混在（分割元）
│   ├─ LINE record - upto_<日時>_<実行日時>_<全体>.tsv       # 全体グループに分割後
│   └─ LINE record - upto_<日時>_<実行日時>_<個別集団>.tsv   # 個別集団グループに分割後
│
├─ all_payload_<全体>/                   # 全体グループの累積ペイロード（過去分を蓄積）
├─ all_payload_<個別集団>/               # 個別集団グループの累積ペイロード
│
├─ <抽出日>_<全体>/                      # 全体グループ フェーズ1の抽出先フォルダ
├─ <抽出日>_<個別集団>/                  # 個別集団グループ フェーズ1の抽出先フォルダ
│
├─ file_cached/                          # 全グループ共有のキャッシュ（添付の補完用）
│                                        #   ファイル名: <messageId>_<groupId>.<拡張子>
│
├─ output_message_pair_<全体>.json               # 全体グループ マスター（累積）
├─ output_message_pair_<全体>_<日時>.json        # 全体グループ 実行結果
├─ output_message_pair_<個別集団>.json           # 個別集団グループ マスター（累積）
└─ output_message_pair_<個別集団>_<日時>.json    # 個別集団グループ 実行結果
```

**ポイント:**

* **設定ファイル・ペイロードフォルダ・抽出先フォルダ・ペア情報 JSON** は
  グループごとに分ける。
* **`Line record/`** には、`read_line_tsv_split_by_group.py` で
  groupId ごとに分割した TSV を置き、グループ別に処理する
  （分割後のファイル名末尾に groupId が付く）。
* **`file_cached/`** は全グループ共有でよい。ファイル名が
  `<messageId>_<groupId>` 形式のため、グループが混在しても取り違えない。
  LINE 側で取得できなくなった添付ファイルを、ここから補完する。
* 各グループについて、本書の**フェーズ1 → 手作業コピー → フェーズ2**を
  それぞれ実行する。


### read_line_tsv_split_by_group.py を実行して、ファイルを分割します。

LINE record - upto_<yyyymmdd_hhmmダウンロード>.tsv の分割元ファイルを指定して実行すると、グループ毎に分割されたファイルが作成されます。<br>
LINE record - upto_<yyyymmdd_hhmmダウンロード>_<yyyymmdd_hhmmss>_<groupId(全体)>.tsv<br>
LINE record - upto_<yyyymmdd_hhmmダウンロード>_<yyyymmdd_hhmmss>_<groupId(個別集団)>.tsv

のようにグループ毎に分割されます